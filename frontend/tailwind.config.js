/**
 * CodeZone - Yohaku Design System
 * Tailwind CSS v4 configuration.
 * Theme tokens are defined in src/app/globals.css via @theme blocks.
 * This config declares the tailwindcss-animate plugin.
 */
export default {
  plugins: [
    require('tailwindcss-animate'),
  ],
}
